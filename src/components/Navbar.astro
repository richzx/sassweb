---
  const links = [
    { title: "Products", path: "/" },
    { title: "Solutions", path: "/solutions" },
    { title: "Services", path: "/services" },
    { title: "Help Center", path: "/helpcenter" },
    { title: "Pricing", path: "/pricing" }
  ];
---

<header class="w-full h-24 px-5 flex items-center justify-between border-b border-slate-300">
  <span class="font-bold text-2xl text-gray-800">Magnificus</span>
  <div class="hidden lg:flex items-center gap-6">
    {links.map((link) => {
      return (
        <a class="font-medium text-[15px]" href={link.path}>{link.title}</a>
      )
    })}
  </div>
  <div class="flex items-center gap-5">
    <a href="/login" class="text-blue-600 font-semibold">Log in</a>
    <button class="bg-blue-600 text-white rounded-lg w-36 h-12">Sign Up for Free</button>
  </div>
</header>
